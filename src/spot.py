class spot():
    def __init__(self, x, y, pixels, tot_intensity):
        self.x = x
        self.y = y
        self.pixels = pixels
        self.total_intensity = tot_intensity 
        
# FIXME: decide whether it is worth to object orient this 
# spotlist = [FileID, CellID, x, y, pixels, f.tot, f.sig, f.median, f.mad, time, FileID_old]

